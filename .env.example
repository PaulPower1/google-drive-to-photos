# Google OAuth 2.0 Credentials
# Get from: Google Cloud Console > APIs & Services > Credentials > Create Credentials > OAuth client ID
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# Optional: Specify a folder ID to start scanning from (leave empty for root)
GOOGLE_DRIVE_FOLDER_ID=
